# ColorSnip
A Windows-based JavaFX color picker with a subtle snip sound, minimal UI, and automatic copying of picked colors to the clipboard. 

Snip hotkey is `Ctrl+Shift+Q`.

## Installation
1. Go to the [v1.1.0 release](https://github.com/Explement/colorsnip/releases/tag/v1.1.0) and download `colorsnip-image.zip`.
2. Extract the ZIP.
3. Open the `bin` folder:
   - **Normal use:** Run `colorsnip.vbs`.
   - **Debug:** Run `colorsnip.bat` to see console output if the app crashes.
